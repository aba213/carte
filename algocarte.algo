algocarte

 var
 n = INTEGER;
 begin 
 for i=1 to n-1 {
   int current = A[i];
   j = i ;


    while(j> 0) and A[j-1] > current{

        swap(A[j-1], A[j]);

      return  j = j -1;
 end for
    }
    end
 }
